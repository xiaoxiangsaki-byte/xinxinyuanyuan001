# 欣原监督系统产品框架

版本：0.1  
定位：自律工具 + 角色陪伴 + 用户与单一主监管者的长期关系系统

## 1. 产品核心

用户不是同时被多个监管者管理，而是从监管者列表中选择一位主监管者。绑定完成后，只有这位主监管者拥有以下权限：

- 查看和监督用户的任务
- 审核任务与处罚证明
- 生成每日、主线、支线任务
- 发送提醒、鼓励、批评和剧情消息
- 记住用户的偏好、成长和重要事件
- 决定奖励、处罚和任务难度

其他监管者在绑定后不参与用户的任务、消息、奖励、处罚和记忆。

## 2. 主监管者绑定规则

1. 用户浏览监管者列表，查看人设、擅长方向、说话风格和任务风格。
2. 用户选择一位监管者并首次确认。
3. 首次确认后，系统保留一次“返回”机会。
4. 点击返回后，用户可以重新选择另一位监管者；返回机会只能使用一次。
5. 第二次确认后，监管者绑定永久锁定。
6. 锁定后，聊天中不能通过指令更换监管者。
7. 其他监管者只保留在资料库中，不产生任何实际行为。

建议补充保护规则：

- 返回发生在正式任务开始前。
- 一旦用户提交过第一项任务，返回机会自动作废。
- 绑定日志必须记录：首次选择、是否使用返回、最终监管者、锁定时间。

内测规则：

- 内测期间不执行永久锁定，主监管者可以重新选择。
- 正式版计划在珍藏阁加入“监管者体验卡”，有效期 3 天。
- 体验卡只能使用饼干兑换，不能使用小奶糕。

## 3. 页面框架

### 任务

- 每日任务、主线任务、支线任务
- 任务提交和监管者审核
- 邮箱、日记、珍藏阁、臻品阁和时光笺入口
- 打卡和处罚任务

### 专注

- 选择关联任务
- 25、45、60 分钟专注计时
- 专注暂停、结束和中断记录
- 后续加入严格锁定和黑白名单

### 每日总结

- 核心任务达标进度
- 今日完成任务记录
- 专注分钟和中断次数
- 监管者评价
- 后续加入时间分布和月度总结

### 资产统计

- 主监管者与更换记录
- 玩家档案
- 小奶糕和饼干
- 资产统计
- 订阅和充值入口

聊天模式通过点击主页面上方监管者栏进入，不占用底部导航位置。

## 4. 核心闭环

### 4.1 绑定闭环

选择监管者 -> 首次确认 -> 可用一次返回 -> 再次选择 -> 最终确认 -> 永久锁定 -> 仅该监管者拥有管理权限。

### 4.2 任务闭环

生成任务 -> 用户执行 -> 上传证明和文字汇报 -> 监管者审核 -> 通过后更新进度 -> 完成全部次数 -> 发放奖励 -> 固定完成状态 -> 更新成长记录。

当前内测规则：

- 每日任务固定为 2 项。
- 主线任务保持 4 项，承担主要成长方向。
- 每日核心任务由“每日任务 + 主线任务”组成。
- 核心任务至少完成 50%，且不少于 3 项。
- 其中必须至少完成 1 项主线任务。

任务未通过：

审核失败 -> 说明原因 -> 用户修改或重新提交 -> 再次审核。

### 4.3 奖励闭环

任务未完成时只显示“提交任务”。  
审核通过后才出现“领取奖励”。  
奖励被领取后变为“已领取”，刷新页面不能恢复成未领取状态。

### 4.4 提醒闭环

监管者按用户时区检查任务 -> 长时间无行动 -> 发送角色化提醒 -> 记录提醒时间 -> 进行冷却 -> 仍未行动则逐级加强消息。

提醒分级：

1. 轻度提醒：一段时间没有操作任务。
2. 当日提醒：今天没有任何完成任务。
3. 连续未完成：一天以上没有完成记录。
4. 严重提醒：连续多天未完成，触发处罚评估。

### 4.5 处罚闭环

未完成任务 -> 监管者生成处罚 -> 用户提交完成证明 -> 监管者审核 -> 审核通过后处罚结束。

如果处罚未按时完成：

处罚继续有效 -> 次日正常任务需求加倍 -> 完成处罚后才能解除加倍。

### 4.6 监督与闲聊闭环

用户有未完成的固定任务时，主监管者进入监督模式：

- 不允许无目的闲聊
- 只处理任务、困难、修改申请和必要沟通
- 用户完成任务或达到监管者认可的进度后，开放聊天模式

监管者需要在聊天中体现：

- 记住过去的重要内容
- 不重复使用相同的话
- 能指出用户的英语、算术、逻辑或概念错误
- 保持人设，不突然变成通用客服

### 4.7 消息模式闭环

聊天模式：短句，一至两句，像真人即时聊天。  
剧情模式：长篇正文，留白排版，按章节或场景推进。  
监督模式：直接、严格，以行动和任务为核心。

所有模式都需要防止代码符号泄漏、重复台词和无意义水话。

### 4.8 记忆闭环

聊天消息 -> 保留近期原始对话 -> 定期压缩成长期摘要 -> 提取少量精华事实 -> 后续对话自动引用。

记忆采用三层结构：

1. 最近 240 条原始对话，保证短期连续性。
2. 自动压缩的长期摘要，记录用户状态和最近变化。
3. 最多 40 条精华事实，优先记录目标、偏好、禁忌、承诺和重要关系。

记忆提取在后台进行，不要求用户手工维护记忆库。

日记隐私：

- 仅自己可见
- 允许主监管者查看
- 只有用户明确允许后，监管者才能引用日记内容

### 4.9 无宠物模式

当前产品不再包含宠物养成、宠物粮食、宠物状态和宠物互动。

保留轻量成长机制，但成长必须服务于监督、任务和长期陪伴，不额外增加养成负担。

### 4.10 商业化闭环

推荐优先采用“平台额度制”：

- 用户充值购买额度
- 聊天、图片识别和任务审核按实际模型用量扣除
- 每月订阅获得固定额度
- 额度用完可以单次充值
- 所有额度和消费在服务器端记录

“我的”页面预留订阅与充值入口。内测期间不扣真实额度，小奶糕固定为 99999999。

平台模型密钥只能放在服务器端。用户自带 API Key 可以保留为高级选项，不能作为普通用户的默认体验。

### 4.11 双货币体系

- 小奶糕：基础货币，通过完成任务免费获得，用于礼物、商店道具、监管者更换权限和普通互动。
- 饼干：高级货币，只能通过充值或订阅获得，用于日常聊天、工作模式聊天和模型消耗。
- 两种货币统一显示在“资产统计 > 财产”。
- 内测期间不扣除真实饼干。

### 4.12 用户档案与模型选择

用户档案包含：

- 用户代号，也是监管者对用户的称呼。
- 用户头像。
- 爱好、习惯偏好、不喜欢或需要避开的内容。

档案内容会写入隐藏记忆，并在后续聊天中自动读取。

聊天输入框左侧使用一个合并按钮，点击后可以选择：

- 相机和图片上传。
- 聊天模型。

模型按饼干标注价格。内测期间展示价格但不扣除真实饼干，正式接入计费后再由服务器扣费。

### 4.13 信件与信封

- 每封信在创建时记录监管者 ID、显示名字、签名和当时的人设文风。
- 旧监管者留下的信件保持原始署名和文风。
- 更换监管者后，新生成的信件必须重新读取当前监管者档案。
- 信封页面只显示关闭、阅读或领取奖励，不允许出现任务提交按钮。

### 4.14 监管者独立主题

- 欣原：保留原紫色主题。
- 林雾：暗红主题。
- 奥斯汀：银灰主题。
- 白砚：黑金主题。

主题统一控制背景、面板、导航、按钮、聊天气泡和进度组件，保证模块颜色协调。

## 5. 用户操作顺序

1. 注册或首次打开。
2. 设置国家、时区、作息和隐私。
3. 选择目标、频率、难度、休息日和限制。
4. 浏览并选择主监管者。
5. 首次确认。
6. 使用或不使用唯一的返回机会。
7. 最终确认并锁定主监管者。
8. 生成初始每日、主线和支线任务。
9. 每天查看今日任务并提交证明。
10. 等待审核和领取奖励。
11. 未完成时进入处罚流程。
12. 完成任务后解锁聊天或剧情互动。
13. 系统持续记录任务、专注、成长和记忆事件。

## 6. 开发顺序

### 第一阶段：先把现有网页稳定下来

- 修复信封中出现上传文字
- 修复奖励领取按钮出现时机
- 修复任务完成后刷新丢失
- 修复打卡天数错误
- 修复重复台词和代码符号
- 修复豁免有效期
- 所有按钮增加明确状态和点击反馈

### 第二阶段：PWA 软件外壳和多个页面

- 四页底部导航：任务、专注、每日总结、资产统计
- 监管者选择页
- 一次性返回和最终锁定
- 监督模式与聊天模式分开
- 选项式任务初始化
- 手机桌面图标和全屏运行

### 第三阶段：账号和云端数据

- 注册和登录
- 用户资料
- 时区和国家
- 云端任务、消息、日记和记忆
- 多设备同步
- 行级权限保护

### 第四阶段：主监管者运行闭环

- 定时检查和提醒
- 任务审核
- 奖励和处罚
- 日记隐私
- 长期记忆
- 严格模式限制闲聊

### 第五阶段：后台推送

- 手机锁屏通知
- 服务端定时任务
- 按用户时区发送
- 消息冷却和分级提醒
- 点击通知后直达对应页面

### 第六阶段：额度和订阅

- 服务端 AI 代理
- 用量统计
- 月度订阅
- 单次充值
- 防重复扣费
- 账单和异常处理

### 第七阶段：安装包

- Android 安装包
- iOS 安装包
- 应用商店合规
- 推送证书和支付渠道

## 7. 暂缓项目

成人模式暂时不进入核心版本。  
原因：年龄验证、地区法规、应用商店政策和支付渠道风险较高。

可以预留关系等级和成熟内容偏好，但首版只做暧昧、陪伴和适度浪漫表达，不做明确色情内容。

## 8. 当前必须遵守的产品原则

- 同一用户只能有一位主监管者。
- 主监管者绑定后，其他监管者没有实际权限。
- 返回机会只能使用一次。
- 任务、奖励、打卡和豁免状态必须持久化。
- 奖励只在审核通过后出现。
- 提醒和调度按用户时区运行。
- 记忆、日记和隐私必须有明确边界。
- 平台密钥绝不放在客户端。
- 每项新需求先判断属于哪个阶段，再决定是否立即加入。
